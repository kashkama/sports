<!DOCTYPE html>
<div class="container">
  <div class="row">
    <p><%= @sport.name%></p><hr/>
  </div>
  <div class="row">
    <%= render("/sports/sports")%>
  </div>
</div><hr/>
<%= link_to 'Edit', edit_sport_path(@sport) %> |
<%= link_to 'Back', sports_path %>
