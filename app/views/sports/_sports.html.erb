<!DOCTYPE html>
<div class="container">
    <h3 class="text-center">Teams</h3>
    <% if @sport.teams.any?%>
    <ul>
      <% @sport.teams.each do |team|%>
        <li><%= team.name%></li>
        <li><%= team.points%></li>
        <li><%= team.win%></li>
        <li><%= team.loss%></li>
        <li><%= team.member%></li>
      <%end%>
    </ul>
    <%else%>
      <p>Add more teams by signing in as a team leader!</p>
    <%end%>
</div>
