<!DOCTYPE html>
  <div class="container">
    <div class="row">
      <h3>sports</h3>
      <ul>
        <% @sports.each() do |sport|%>
          <li><%= image_tag sport.image.url(:medium) %>|<%= link_to sport.name, sport_path(sport)%></li>
        <%end%>
      </ul>
    </div>
  </div>
<%= link_to 'New Sport', new_sport_path %>
